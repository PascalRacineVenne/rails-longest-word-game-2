<div class="container">
  <h1>Your Score</h1>

  <%# if else statement or case depending on included and is_english %>
  <% if @included %>
    <% if @is_english %>
      <p><strong>Congratulations!</strong> <%= @word.upcase %> is an English word</p>
    <% else %>
      <p>Sorry but <%= @word.upcase %> doesn't seem to be a valid english word...</p>
    <% end %>
  <% else %>

    <p> Sorry but <strong><%= @word.upcase %></strong> cannot be built out of these letters.</p>
  <% end %>
  <div class="score">
    <h2 class="points">Points: <%= @points %></h2>
    <%= link_to 'Play Again', new_path, class: "btn-link" %>  
  </div>
 </div> 